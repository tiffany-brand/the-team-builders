<!-- Navbar -->
<nav class="container-fluid bg-white border-bottom border-dark">
    <div class="row align-items-center">
        <div class="col"></div>
        <div class="col-md-5 d-flex justify-content-md-start justify-content-center">
            <img src="/assets/img/mainlogo.png" alt="Team Builders" class="logoimg" />
        </div>
        <div class="col-md-6 mb-2 mb-md-0 mr-md-2">
            <ul class="navbar-nav flex-row justify-content-md-end justify-content-center">
                {{#if user }}
                <li class="nav-item">
                    <a class="nav-link px-2 mx-2 navstyle" href="/dashboard">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-0 mt-2 border-left current-nav navstyle" href="/user">Profile<span
                            class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-0 mt-2 border-left border-right navstyle" href="/cardgame">Game</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-2 mx-2 navstyle" href="/logout">Logout</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link px-2 mx-2 current-nav navstyle" href="/">Home <span
                            class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 py-0 mt-2 border-left navstyle" href="/login" tabindex="-1"
                        aria-disabled="true">Log In</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>

<!-- Welcome Message -->
<section class="container-fluid bg-lt-blue py-5">
    <div class="row py-3 py-md-4">
        <div class="col">
            <h1 class="text-white text-center h1style">Update your profile, <span
                    class="text-purple">{{user.displayName}}</span></h1>
        </div>
    </div>
</section>

<!-- Profile Content -->
<section class="container-fluid bg-light">
    <div class="row">
        <div class="col"></div>
        <div class="col-lg-3 mt-3 d-flex flex-column justify-content-center text-center">
            <div class="card border border-purple mx-auto">
                <img src={{user.picture}} class="card-img-top" alt="Profile Picture">
                <div class="card-body">
                    <h5 class="card-title">{{user.displayName}}</h5>
                    <h6 class="card-subtitle mb-1 text-muted">Nickname: {{user.nickname}}</h6>
                    <p class="card-text">Email: {{user.email}}</p>
                </div>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="container bg-white mt-lg-n5 mt-4 mt-lg-0 pt-2 border border-purple">
                <form class="submitForm">
                    <div class="form-row">
                        <div class="form-group col-4">
                            <label for="input" class="col-form-label">First Name </label>
                            <input type="text" class="form-control" data-id={{user.id}} id="firstname">
                        </div>
                        <div class="form-group col-4">
                            <label for="input" class="col-form-label">Last Name</label>
                            <input type="text" class="form-control" id="lastname">
                        </div>
                        <div class="form-group col-4">
                            <label for="input" class="col-form-label">Nickname</label>
                            <input type="text" class="form-control" id="nickname">
                        </div>
                    </div>
                    <div class="form-row">
                        {{#each user.userQa}}
                        <div class="form-group col-6">
                            <label for="input" class="col-form-label">{{this.question}}</label>
                            <input name="question-{{this.questionId}}" data-questionid="1" data-answerid="{{this.answerId}}" type="text" class="form-control" id="talent" value="{{this.answer}}">
                        </div>
                        {{/each}}
                    </div>
                    <div class="form-row">
                        <div class="form-group col-6">
                            <label for="inputTeam">Your Team</label>
                            <select id="inputTeam" class="form-control">
                                {{!-- Dropdown options created via JS --}}
                            </select>
                        </div>
                        <div class="form-group col-6">
                            <label for="pictureUpload">Update Profile Picture</label>
                            {{!-- <input type="file" class="form-control-file" id="pictureUpload"> --}}
                            <div>
                            <a href="/image-update" class="btn bg-lt-blue text-white btn-text px-4" id="imgPicker">Update Image</a>
                            </div>
                        </div>
                    </div>
                    <div class="row justify-content-center pb-3">
                        <button type="submit" class="btn bg-lt-blue text-white btn-text px-4 mt-2" id="updateProfile"
                            data-id="{{user.id}}">Save Changes</button>
                    </div>
                </form>

            </div>
        </div>
        <div class="col"></div>
    </div>
</section>

<!-- Footer -->
<footer class="footer-margin bg-light">
    <div class="navbar fixed-bottom navbar-dark bg-purple justify-content-center">
        <span class="navbar-brand footer-font blue align-middle">Copyright &copy;2020
            &ThickSpace;|&ThickSpace;
            Team Builder</span>
    </div>
</footer>